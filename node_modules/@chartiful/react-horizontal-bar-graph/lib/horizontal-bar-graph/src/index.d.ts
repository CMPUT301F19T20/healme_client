/// <reference types="react" />
import type { VerticalBarGraphConfig } from '../../types';
declare const HorizontalBarGraph: ({ data, labels, height, width, barRadius, barWidthPercentage, barColor, style, baseConfig }: VerticalBarGraphConfig) => JSX.Element;
export default HorizontalBarGraph;
